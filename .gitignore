# Back-end files
back-end/node_modules    # dependências (pesado, recriável com npm install)
back-end/.env             # variáveis de ambiente secretas
back-end/npm-debug.log*   # logs de erro do npm
back-end/dist             # pasta de build (se usar build/transpile)
back-end/coverage         # relatórios de testes (ex.: jest --coverage)
